
def visibility(state, object, camera):
    """ Returns if a given object is visible from a given camera.

    :param state: the geometric state where the check must be done
    :param object: the object id
    :param camera: the camera id

    :returns: a boolean
    """
    return False
